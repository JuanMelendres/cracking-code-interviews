# Week 10 PostgreSQL — Outbox Schema

`setup.sql` creates the `orders` and `outbox` tables used by the working transactional-outbox implementation in `practice/java/week-10/outbox-publisher/`.

See `study-packs/week-10/08-outbox-implementation-deliverable.md` for the full walkthrough, schema rationale, and exact reproduce sequence — this directory holds only the schema, the actual demonstration lives in the Java implementation.
