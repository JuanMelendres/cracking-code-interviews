// A real, single-token mutation: >= mutated to > (a "boundary" mutant, the
// most common real mutation operator in tools like PIT).
public class DiscountPolicy {
    static boolean isEligibleForDiscount(int orderTotal) {
        return orderTotal > 100;
    }
}
